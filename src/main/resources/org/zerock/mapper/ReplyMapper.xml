<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.ReplyMapper">

	<insert id="insert">
	<![CDATA[
		insert into tbl_reply (rno, bno, reply, replyer) values (seq_reply.nextval, #{bno}, #{reply}, #{replyer})
	]]> <!-- CDATA : sql 파일에 부등호 계산을 db에서 하게끔 (모든 글자를 문자열처리해줌) -->
	</insert> <!-- result = 성공1, 실패0 -->

	<select id="read" resultType="org.zerock.domain.ReplyVO">
	<![CDATA[
		select * from tbl_reply where rno = #{rno}
	]]>
	</select>

	<update id="update">
	<![CDATA[
		update tbl_reply set reply = #{reply}, updateDate = sysdate where rno = #{rno}
	]]>
	</update>

	<delete id="delete">
	<![CDATA[
		delete from tbl_reply where rno = #{rno}
	]]>
	</delete>

	<select id="getList" resultType="org.zerock.domain.ReplyVO">
	<![CDATA[
		select * from tbl_reply where rno > 0
	]]>
	</select>


	<!-- 페이징 + bno를 이용한 리스트 출력용 -->
	<select id="getListWithPaging"
		resultType="org.zerock.domain.ReplyVO">
	<![CDATA[
		select rno, bno, reply, replyer, replyDate, updateDate from 
(select /*+INDEX(tbl_reply idx_reply) */ rownum rn, rno, bno, reply, replyer, replyDate, updateDate from tbl_reply
where bno = #{bno} and rno > 0 and rownum <= #{cri.pageNum} * #{cri.amount})
		where rn > (#{cri.pageNum} -1) * #{cri.amount}
	]]>
	</select>
	
	<select id="getCountByBno" resultType="int">
	<![CDATA[
		select count(rno) from tbl_reply where bno = #{bno}
	]]>
	</select>





</mapper>